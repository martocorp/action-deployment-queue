extends: default

rules:
  # 120 chars should be enough, but don't fail if a line is longer
  line-length:
    max: 120
    level: warning

  # Allow "on" keyword for GitHub Actions workflows and quoted strings for defaults
  truthy:
    allowed-values: ['true', 'false', 'on']
    check-keys: false
